{
  "name": "commodity-dashboard-proxy",
  "version": "1.0.0",
  "description": "Local proxy server + static frontend for live commodity dashboard (metals + WTI).",
  "main": "server.js",
  "type": "commonjs",
  "private": true,
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "node-fetch": "^2.7.0"
  }
}
